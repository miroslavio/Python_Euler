keylogs = []
with open('keylog.txt', 'r') as f:
    for line in f.readlines():
        keylogs.append(int(line))

digits = [0,1,2,3,6,7,8,9]

# Find first digit
for keylog in keylogs:
    keys = []
    while keylog:
        keys.append
# solved by hand
    
